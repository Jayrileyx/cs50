# Get input from user
text = input("Please provide input to playback. ")

# Create variable to replace with
replacement_character = "..."

# Find replace in documentation
new_text = text.replace(" ", replacement_character)
print(new_text)
